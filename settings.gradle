// settings.gradle

pluginManagement {
    repositories {
        gradlePluginPortal()
        google() // Essential for AndroidX and Google libraries
        mavenCentral() // Essential for many other libraries
    }
}
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // Recommended for consistency
    repositories {
        google() // <--- ADD THIS LINE
        mavenCentral() // <--- ADD THIS LINE
    }
}

include ':app'
rootProject.name = "Digital Quadrant"